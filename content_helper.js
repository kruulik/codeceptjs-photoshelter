
'use strict';

class Content extends Helper {

  // before/after hooks
  _before() {
    let 
    let email = 'karolis_test' + time + '@gmail.com';
  },
  const time = new Date().toLocaleTimeString().replace(/[^0-9a-zA-Z]/g, '');
  const email = 'karolis_test'+ time + '@gmail.com';
  

}

module.exports = Content;
